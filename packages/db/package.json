{
  "name": "@nutrios/db",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "seed:dev": "tsx src/seeds/dev.seed.ts",
    "seed:canasta": "tsx src/seeds/canasta-mexicana.seed.ts",
    "seed:verify": "tsx src/seeds/verify.seed.ts",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "@nutrios/config": "workspace:*",
    "prisma": "^6.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.3"
  }
}
